"""addon — KiCad IPC/addon-facing layer.

This package contains only thin wrappers around rfcore functionality.
It depends on the kicad-python package and a running KiCad 9+ instance.
No RF modeling code should live here.
"""
