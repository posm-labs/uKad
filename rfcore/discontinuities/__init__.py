"""Discontinuity block models for microstrip endpoint transitions.

Each block is a separate two-port that produces an ABCD matrix at a given frequency.
Blocks are cascaded with the taper body to form the full chain.
"""
