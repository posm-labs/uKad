"""Export modules for taper data.

Submodules:
  csv_export   — CSV frequency-domain and geometry data
  touchstone   — Touchstone 2.0 and 1.0 S-parameter files
  geometry     — Polygon, SVG, PNG preview, .kicad_mod fallback
  openems_adapter — openEMS EM solver adapter (future)
"""
